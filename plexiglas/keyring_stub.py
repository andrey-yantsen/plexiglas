def get_password(service_name, username):
    from . import log
    log.warning('get_password() on stub-keyring')


def set_password(service_name, username, password):
    from . import log
    log.warning('set_password() on stub-keyring')

